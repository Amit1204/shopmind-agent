# ── ShopMind Local Requirements (CPU-only, no Ollama) ─────────────────────────
# Install: pip install -r requirements-local.txt

# PyTorch (Mac default is CPU-only — no suffix needed)
torch==2.1.0
torchvision==0.16.0

# LangChain + Groq
langchain==0.2.0
langchain-groq==0.1.6
langchain-community==0.2.0
langgraph==0.1.5

# API + UI
fastapi==0.111.0
uvicorn==0.29.0
streamlit==1.35.0

# Vector search
chromadb==0.5.0
faiss-cpu==1.7.4
rank-bm25==0.2.2

# Embeddings + NLP (CPU inference)
sentence-transformers==2.7.0
transformers==4.40.0

# Image support (CLIP)
Pillow==10.3.0
open-clip-torch==2.24.0

# ML models
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.0

# Config + utils
pydantic-settings==2.2.1
python-dotenv==1.0.1
pandas==2.2.2
numpy==1.26.4
tqdm==4.66.2
requests==2.31.0

# Observability
mlflow==2.13.0
